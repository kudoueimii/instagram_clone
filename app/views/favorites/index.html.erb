<h1>お気に入り一覧</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fav.each do |favorite| %>
      <tr>
        <td><%= image_tag favorite.image.to_s, size: '200x200'%></td>
        <th><%= favorite.content%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', posts_path %>
